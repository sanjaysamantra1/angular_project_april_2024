import { GenderPrefixPipe } from './gender-prefix.pipe';

describe('GenderPrefixPipe', () => {
  it('create an instance', () => {
    const pipe = new GenderPrefixPipe();
    expect(pipe).toBeTruthy();
  });
});
